package com.authx.testapp.schema;

import com.authx.sdk.AuthxClient;
import com.authx.sdk.TypedResourceFactory;
import com.authx.sdk.TypedGrantAction;
import com.authx.sdk.TypedRevokeAction;
import com.authx.sdk.model.GrantResult;
import com.authx.testapp.schema.constants.Department;

/**
 * Type-safe resource factory for <b>department</b>.
 * Generated by AuthxCodegen — do not edit.
 *
 * <pre>
 * DepartmentResource doc = client.department();
 * doc.grant("id", Department.Rel.EDITOR).toUser("bob");
 * doc.check("id", Department.Perm.VIEW, "alice");
 * </pre>
 */
public class DepartmentResource extends TypedResourceFactory<Department.Rel, Department.Perm> {

    public DepartmentResource(AuthxClient client) {
        init("department", client);
    }

    @Override
    public DepartmentGrantAction grant(String id, Department.Rel relation) {
        return new DepartmentGrantAction(this, id, relation);
    }

    @Override
    public DepartmentRevokeAction revoke(String id, Department.Rel relation) {
        return new DepartmentRevokeAction(this, id, relation);
    }

    /** Typed grant action with subject-specific to*() methods. */
    public static class DepartmentGrantAction extends TypedGrantAction<Department.Rel> {

        public DepartmentGrantAction(DepartmentResource factory, String id, Department.Rel relation) {
            super(factory, id, relation);
        }

        /** Grant to department:{id}. */
        public void toDepartment(String... ids) {
            String[] refs = new String[ids.length];
            for (int i = 0; i < ids.length; i++) refs[i] = "department:" + ids[i] + "";
            factory.grantToSubjects(id, relation.relationName(), refs);
        }

        /** Grant to user:{id}. */
        public void toUser(String... ids) {
            String[] refs = new String[ids.length];
            for (int i = 0; i < ids.length; i++) refs[i] = "user:" + ids[i] + "";
            factory.grantToSubjects(id, relation.relationName(), refs);
        }

    }

    /** Typed revoke action with subject-specific from*() methods. */
    public static class DepartmentRevokeAction extends TypedRevokeAction<Department.Rel> {

        public DepartmentRevokeAction(DepartmentResource factory, String id, Department.Rel relation) {
            super(factory, id, relation);
        }

        /** Revoke from department:{id}. */
        public void fromDepartment(String... ids) {
            String[] refs = new String[ids.length];
            for (int i = 0; i < ids.length; i++) refs[i] = "department:" + ids[i] + "";
            factory.revokeFromSubjects(id, relation.relationName(), refs);
        }

        /** Revoke from user:{id}. */
        public void fromUser(String... ids) {
            String[] refs = new String[ids.length];
            for (int i = 0; i < ids.length; i++) refs[i] = "user:" + ids[i] + "";
            factory.revokeFromSubjects(id, relation.relationName(), refs);
        }

    }

}
