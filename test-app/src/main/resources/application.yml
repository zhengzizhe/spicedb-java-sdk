server:
  port: 8080

spring:
  application:
    name: authx-testapp

authx:
  spicedb:
    target: localhost:50051
    token: localdevlocaldevlocaldevlocaldev
    tls: false

management:
  endpoints:
    web:
      exposure:
        include: health, info, metrics, prometheus
